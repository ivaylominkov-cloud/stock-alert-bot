name: Stock Bot

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 2   # ✅ prevents hanging

    steps:
      - run: echo "Starting..."
      - run: python --version
